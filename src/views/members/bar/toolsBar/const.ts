// 枚举
import { LayerTypeEnum } from '@/enum/map'
const DEFAULT_LAYER_ITEM: { group: number; layerType: LayerTypeEnum; val: string } = {
    group: 2,
    layerType: LayerTypeEnum.GROUP_PATH_LAYER,
    val: '集合路径'
}
export { DEFAULT_LAYER_ITEM }
